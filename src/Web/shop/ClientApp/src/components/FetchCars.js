import apiUrl from './api';

class ChildComponent extends React.Component {
    fetchCars() {
      // TODO: Make implementation so it fetch a request and retrive the products, 
      // call this method in product page and also when editing row  
    }
}